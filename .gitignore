# Cached Sleeper player map (~5 MB) — restored by the Actions cache, never committed
.cache/

# Local build output. The workflow builds to the repo root instead (that is what
# GitHub Pages serves); `--out site` is just the convenient default for local runs.
site/
build.log

__pycache__/
*.py[cod]
.venv/
